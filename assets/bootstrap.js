import { startStimulusApp } from '@symfony/stimulus-bundle';

const app = startStimulusApp();

// Enregistrez ici vos contrôleurs Stimulus personnalisés
// app.register('my-controller', MyController);
